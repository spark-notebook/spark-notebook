
CodeMirror

2.32, downloaded by Chris Yang 8/8/2012.

We didn't apply the python patch (referenced in README-IPython.rst) but we added a CSS theme (iPython) to the theme directory, so that won't match.  Otherwise, it should exactly the same as downloading from the internet and unzipping.